<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="MiFondo">
<meta name="theme-color" content="#0a0a0f">
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="icon-192.png">
<title>Control de Finanzas</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700&family=Space+Mono:wght@400;700&family=Syne:wght@400;600;700;800&display=swap');
  :root {
    --bg:      #0a0a0f;
    --surface: #13131a;
    --sur2:    #1c1c27;
    --border:  #2a2a3a;
    --green:   #00e5a0;
    --red:     #ff6b6b;
    --gold:    #ffd166;
    --text:    #e8e8f0;
    --muted:   #a8a8c0;
    --r:       18px;
  }
  *{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
  body{font-family:'Syne',sans-serif;background:var(--bg);color:var(--text);min-height:100vh;padding-bottom:40px}

  /* HEADER */
  .hdr{background:var(--surface);border-bottom:1px solid var(--border);padding:16px 16px 14px;position:sticky;top:0;z-index:50}
  .hdr-top{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px}
  .logo{font-size:44px;font-weight:800;letter-spacing:-0.5px}
  .logo span{color:var(--green)}

  /* Botón sincronizar */
  .btn-sync{background:none;border:1px solid var(--border);border-radius:10px;
    padding:8px 12px;font-family:'Syne',sans-serif;font-size:22px;font-weight:700;
    color:var(--muted);cursor:pointer;display:flex;align-items:center;gap:6px;
    transition:all .2s;white-space:nowrap}
  .btn-sync.pendiente{border-color:var(--gold);color:var(--gold);animation:pulse-gold 2s infinite}
  .btn-sync.sincronizando{border-color:var(--green);color:var(--green)}
  .sync-dot{width:8px;height:8px;border-radius:50%;background:var(--border);flex-shrink:0;transition:background .2s}
  .btn-sync.pendiente .sync-dot{background:var(--gold)}
  .btn-sync.sincronizando .sync-dot{background:var(--green)}
  @keyframes pulse-gold{0%,100%{opacity:1}50%{opacity:.55}}

  /* Selector de fecha */
  .fecha-row{display:flex;align-items:center;gap:8px;margin-bottom:12px}
  .fecha-nav{background:var(--sur2);border:1px solid var(--border);border-radius:10px;
    width:40px;height:40px;display:flex;align-items:center;justify-content:center;
    font-size:40px;cursor:pointer;color:var(--text);flex-shrink:0}
  .fecha-nav:active{opacity:0.7}
  .fecha-display{flex:1;background:var(--sur2);border:1px solid var(--border);border-radius:10px;
    padding:9px 14px;font-family:'Space Mono',monospace;font-size:30px;font-weight:700;
    color:var(--green);text-align:center;cursor:pointer}
  .fecha-display.pasado{color:var(--gold)}

  /* Tasa */
  .tasa-row{display:flex;align-items:center;gap:8px}
  .tasa-lbl{font-size:44px;color:var(--text);white-space:nowrap;font-weight:700}
  .tasa-inp{flex:1;background:var(--sur2);border:2px solid #444466;border-radius:12px;
    padding:10px 14px;font-family:'Space Mono',monospace;font-size:36px;font-weight:700;
    color:var(--gold);text-align:right;outline:none}
  .tasa-inp:focus{border-color:var(--gold)}
  .btn-tasa{background:var(--gold);color:#000;border:none;border-radius:12px;
    padding:10px 16px;font-family:'Syne',sans-serif;font-size:30px;font-weight:700;cursor:pointer;white-space:nowrap}

  /* TABS */
  .tabs{display:flex;gap:8px;padding:14px 16px 0}
  .tab{flex:1;background:var(--surface);border:2px solid #444466;border-radius:12px;
    padding:12px;font-family:'Syne',sans-serif;font-size:38px;font-weight:700;
    color:var(--text);cursor:pointer;text-align:center;transition:all .15s}
  .tab.active{background:var(--sur2);border-color:var(--green);color:var(--green)}

  /* SECTION */
  .sec{display:none}.sec.active{display:block}

  /* CARDS */
  .grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;padding:14px 16px}
  .card{background:var(--surface);border:1px solid var(--border);border-radius:var(--r);padding:14px}
  .card.full{grid-column:1/-1}
  .clbl{font-size:32px;color:var(--muted);margin-bottom:5px;text-transform:uppercase;letter-spacing:.5px}
  .cval{font-family:'Space Mono',monospace;font-size:40px;font-weight:700;line-height:1.1}
  .g{color:var(--green)}.r{color:var(--red)}.gold{color:var(--gold)}

  /* BTN AGREGAR */
  .wrap-add{padding:0 16px 14px}
  .btn-add{width:100%;background:var(--green);color:#000;border:none;border-radius:var(--r);
    padding:18px;font-family:'Syne',sans-serif;font-size:40px;font-weight:800;cursor:pointer;
    display:flex;align-items:center;justify-content:center;gap:10px;transition:transform .1s,opacity .1s}
  .btn-add:active{transform:scale(.97);opacity:.9}
  .btn-add .plus{font-size:52px;line-height:1}

  /* LISTA */
  .sec-title{font-size:34px;font-weight:700;color:var(--muted);padding:0 16px 10px;text-transform:uppercase;letter-spacing:.5px}
  .mov-list{padding:0 16px;display:flex;flex-direction:column;gap:10px}
  .mov{background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:14px 16px;display:flex;align-items:center;gap:12px}
  .mov-ico{width:44px;height:44px;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:40px;flex-shrink:0}
  .mov-ico.i{background:rgba(0,229,160,.15)}.mov-ico.e{background:rgba(255,107,107,.15)}
  .mov-info{flex:1;min-width:0}
  .mov-con{font-size:34px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .mov-meta{font-size:32px;color:var(--muted);margin-top:2px}
  .mov-amt{font-family:'Space Mono',monospace;font-size:34px;font-weight:700;text-align:right;flex-shrink:0}
  .mov-amt.i{color:var(--green)}.mov-amt.e{color:var(--red)}
  .mov-sub{font-size:24px;color:var(--gold);text-align:right}
  .btn-del{background:none;border:none;color:var(--red);font-size:40px;cursor:pointer;padding:4px;flex-shrink:0}
  .btn-edit{background:none;border:none;color:var(--gold);font-size:40px;cursor:pointer;padding:4px;flex-shrink:0}
  .mov-local{font-size:20px;color:var(--gold);margin-top:3px;display:flex;align-items:center;gap:4px}
  .empty{text-align:center;color:var(--muted);font-size:32px;padding:30px 16px}

  /* HISTORIAL */
  .hist-wrap{padding:14px 16px}
  .htbl{width:100%;border-collapse:collapse;font-family:'Barlow Condensed',sans-serif;font-size:32px;font-weight:600}
  .htbl th{color:var(--muted);text-align:left;padding:8px 4px;border-bottom:1px solid var(--border);font-size:28px;text-transform:uppercase;font-family:'Barlow Condensed',sans-serif;font-weight:700;letter-spacing:.5px}
  .htbl td{padding:10px 4px;border-bottom:1px solid var(--border);font-size:32px;font-family:'Barlow Condensed',sans-serif;font-weight:600}
  .htbl tr:last-child td{border-bottom:none}
  .htbl tr{cursor:pointer}.htbl tr:active td{opacity:.7}

  /* MODAL */
  .overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.92);z-index:100;align-items:flex-start;justify-content:center;overflow-y:auto;padding:16px 16px 40px}
  .overlay.open{display:flex}
  .modal{background:var(--surface);border-radius:28px;padding:24px 20px 32px;
    width:100%;max-width:500px;border:1px solid var(--border);
    animation:up .22s ease;margin:auto}
  @keyframes up{from{opacity:0;transform:translateY(-18px)}to{opacity:1;transform:translateY(0)}}
  .handle{width:40px;height:4px;background:var(--border);border-radius:4px;margin:0 auto 20px}
  .mtitle{font-size:44px;font-weight:800;margin-bottom:18px;letter-spacing:-.5px}
  .plbl{font-size:32px;color:var(--muted);text-transform:uppercase;letter-spacing:.5px;font-weight:700;margin-bottom:10px}
  .dvdr{height:1px;background:var(--border);margin:4px 0 18px}

  /* Tipo */
  .tipo-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:18px}
  .tbtn{padding:18px 10px;border-radius:16px;border:2px solid var(--border);background:var(--sur2);
    font-family:'Syne',sans-serif;font-size:36px;font-weight:800;color:var(--muted);cursor:pointer;text-align:center;transition:all .15s}
  .tbtn .ico{font-size:52px;display:block;margin-bottom:4px}
  .tbtn.ing.sel{border-color:var(--green);color:var(--green);background:rgba(0,229,160,.1)}
  .tbtn.egr.sel{border-color:var(--red);color:var(--red);background:rgba(255,107,107,.1)}

  /* Subtipo ingreso */
  .sub-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:18px}
  .sbtn{padding:16px 8px;border-radius:14px;border:2px solid var(--border);background:var(--sur2);
    font-family:'Syne',sans-serif;font-size:30px;font-weight:700;color:var(--muted);cursor:pointer;text-align:center;transition:all .15s}
  .sbtn .ico{font-size:44px;display:block;margin-bottom:4px}
  .sbtn.sel{border-color:var(--green);color:var(--green);background:rgba(0,229,160,.1)}
  .sbtn.dol.sel{border-color:var(--gold);color:var(--gold);background:rgba(255,209,102,.1)}

  /* Cat egreso */
  .cat-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:18px}
  .cbtn{padding:16px;border-radius:14px;border:2px solid var(--border);background:var(--sur2);
    font-family:'Syne',sans-serif;font-size:34px;font-weight:700;color:var(--muted);cursor:pointer;text-align:center;transition:all .15s}
  .cbtn .ico{font-size:48px;display:block;margin-bottom:4px}
  .cbtn.sel{border-color:var(--red);color:var(--red);background:rgba(255,107,107,.1)}

  /* Concepto egreso */
  .con-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:18px}
  .konbtn{padding:14px 8px;border-radius:12px;border:2px solid var(--border);background:var(--sur2);
    font-family:'Syne',sans-serif;font-size:28px;font-weight:700;color:var(--muted);cursor:pointer;text-align:center;transition:all .15s}
  .konbtn.sel{border-color:var(--red);color:var(--red);background:rgba(255,107,107,.1)}

  /* Monto */
  .fld{margin-bottom:18px}
  .flbl{font-size:34px;color:var(--muted);margin-bottom:8px;text-transform:uppercase;letter-spacing:.5px;font-weight:600}
  .finp{width:100%;background:var(--sur2);border:1px solid var(--border);border-radius:14px;
    padding:18px;font-family:'Space Mono',monospace;font-size:56px;font-weight:700;
    color:var(--gold);outline:none;text-align:right}
  .finp:focus{border-color:var(--gold)}
  .finp::placeholder{color:var(--muted);font-weight:400;font-size:40px}

  .btn-save{width:100%;background:var(--green);color:#000;border:none;border-radius:16px;
    padding:20px;font-family:'Syne',sans-serif;font-size:40px;font-weight:800;cursor:pointer;
    margin-top:4px;transition:transform .1s,opacity .1s}
  .btn-save:active{transform:scale(.97);opacity:.9}
  .btn-can{width:100%;background:none;color:var(--muted);border:none;border-radius:16px;
    padding:16px;font-family:'Syne',sans-serif;font-size:34px;font-weight:600;cursor:pointer;margin-top:4px}

  /* DATEPICKER modal */
  .dp-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.85);z-index:110;align-items:center;justify-content:center}
  .dp-overlay.open{display:flex}
  .dp-box{background:var(--surface);border-radius:24px;padding:24px;width:calc(100% - 40px);max-width:340px;border:1px solid var(--border)}
  .dp-title{font-size:40px;font-weight:800;margin-bottom:18px;text-align:center}
  .dp-inp{width:100%;background:var(--sur2);border:1px solid var(--border);border-radius:14px;
    padding:16px;font-family:'Space Mono',monospace;font-size:40px;font-weight:700;
    color:var(--gold);outline:none;text-align:center;margin-bottom:16px}
  .dp-inp:focus{border-color:var(--gold)}
  .dp-btns{display:grid;grid-template-columns:1fr 1fr;gap:10px}
  .dp-ok{background:var(--green);color:#000;border:none;border-radius:14px;padding:16px;font-family:'Syne',sans-serif;font-size:34px;font-weight:800;cursor:pointer}
  .dp-cancel{background:var(--sur2);color:var(--muted);border:1px solid var(--border);border-radius:14px;padding:16px;font-family:'Syne',sans-serif;font-size:34px;font-weight:700;cursor:pointer}

  /* SPINNER */
  .spin{display:none;position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:200;
    align-items:center;justify-content:center;flex-direction:column;gap:16px;font-size:36px;color:var(--green);font-weight:700}
  .spin.show{display:flex}
  .ring{width:50px;height:50px;border:4px solid var(--border);border-top-color:var(--green);border-radius:50%;animation:sp .8s linear infinite}
  @keyframes sp{to{transform:rotate(360deg)}}

  /* TOAST */
  .toast{position:fixed;bottom:30px;left:50%;transform:translateX(-50%) translateY(20px);
    background:var(--green);color:#000;padding:14px 24px;border-radius:30px;
    font-size:32px;font-weight:700;opacity:0;transition:all .3s;z-index:300;white-space:nowrap}
  .toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
  .toast.err{background:var(--red);color:#fff}
  .toast.warn{background:var(--gold);color:#000}

  /* Banner offline */
  .offline-banner{display:none;background:var(--sur2);border:1px solid var(--gold);
    border-radius:12px;margin:10px 16px 0;padding:10px 14px;
    font-size:26px;color:var(--gold);font-weight:700;text-align:center}
  .offline-banner.show{display:block}
</style>
</head>
<body>

<!-- BANNER OFFLINE -->
<div class="offline-banner" id="offlineBanner">📵 Sin conexión — los datos se guardan localmente</div>

<!-- HEADER -->
<div class="hdr">
  <div class="hdr-top">
    <div class="logo">Mi<span>Fondo</span></div>
    <button class="btn-sync" id="btnSync" onclick="sincronizar()">
      <span class="sync-dot" id="syncDot"></span>
      <span id="syncTxt">Sincronizar</span>
    </button>
  </div>
  <!-- Selector de fecha -->
  <div class="fecha-row">
    <div class="fecha-nav" onclick="cambiarDia(-1)">‹</div>
    <label style="flex:1;position:relative">
      <input type="date" id="dpNativo" style="position:absolute;inset:0;opacity:0;width:100%;height:100%;cursor:pointer" onchange="aplicarFechaNativa(this.value)">
      <div class="fecha-display" id="fechaDisplay">--/--/--</div>
    </label>
    <div class="fecha-nav" onclick="cambiarDia(1)">›</div>
  </div>
  <!-- Tasa -->
  <div class="tasa-row">
    <span class="tasa-lbl">💵 Tasa $</span>
    <input type="text" class="tasa-inp" id="tasaInp" placeholder="0,00" inputmode="decimal">
    <button class="btn-tasa" onclick="guardarTasa()">Guardar</button>
  </div>
</div>

<!-- TABS -->
<div class="tabs">
  <div class="tab active" onclick="showTab('hoy',this)">📋 Día</div>
  <div class="tab" onclick="showTab('hist',this)">📅 Historial</div>
</div>

<!-- TAB DÍA -->
<div class="sec active" id="tab-hoy">
  <div class="grid">
    <div class="card"><div class="clbl">Ingresos Bs</div><div class="cval g" id="cIngBs">Bs 0</div></div>
    <div class="card"><div class="clbl">Ingresos $</div><div class="cval gold" id="cIngDol">$ 0</div></div>
    <div class="card"><div class="clbl">Egresos Casa</div><div class="cval r" id="cEgCasa">Bs 0</div></div>
    <div class="card"><div class="clbl">Egresos Trabajo</div><div class="cval r" id="cEgTrab">Bs 0</div></div>
    <div class="card"><div class="clbl">Saldo del día</div><div class="cval g" id="cSaldo">Bs 0</div></div>
    <div class="card"><div class="clbl">Fondo $ (acum.)</div><div class="cval gold" id="cFondoDol">$ 0</div></div>
    <div class="card"><div class="clbl">Ingresos del día $</div><div class="cval g" id="cIngDiaDol">$ 0</div></div>
    <div class="card"><div class="clbl">Fondo Acumulado Bs</div><div class="cval gold" id="cFondoBs">Bs 0</div></div>
  </div>

  <!-- Totales históricos -->
  <div style="padding:0 16px 4px">
    <div style="font-size:26px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">Totales históricos</div>
  </div>
  <div class="grid" style="padding-top:0">
    <div class="card"><div class="clbl">Total ingresado Bs</div><div class="cval g" id="hTotalIng">Bs 0</div></div>
    <div class="card"><div class="clbl">Total ingresado $</div><div class="cval g" id="hTotalIngDol">$ 0</div></div>
    <div class="card"><div class="clbl">Total egresado Bs</div><div class="cval r" id="hTotalEg">Bs 0</div></div>
    <div class="card"><div class="clbl">Total egresado $</div><div class="cval r" id="hTotalEgDol">$ 0</div></div>
    <div class="card"><div class="clbl">Fondo total $</div><div class="cval gold" id="hFondoDol">$ 0</div></div>
    <div class="card"><div class="clbl">Fondo total Bs</div><div class="cval gold" id="hFondoBs">Bs 0</div></div>
  </div>
  <div class="wrap-add">
    <button class="btn-add" onclick="abrirModal()"><span class="plus">＋</span> Agregar movimiento</button>
  </div>
  <div class="sec-title">Movimientos</div>
  <div class="mov-list" id="movList"><div class="empty">Sin movimientos este día</div></div>
</div>

<!-- TAB HISTORIAL -->
<div class="sec" id="tab-hist">
  <div style="padding:14px 16px 0">
    <button onclick="recalcularTodo()" style="width:100%;background:var(--sur2);color:var(--muted);border:1px solid var(--border);border-radius:14px;padding:16px;font-family:'Syne',sans-serif;font-size:26px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:10px">
      🔄 Recalcular Resumen desde Movimientos
    </button>
  </div>
  <div class="hist-wrap">
    <table class="htbl">
      <thead><tr><th>Fecha</th><th>Ingresos</th><th>Egresos</th><th>Saldo</th><th>Fondo $</th></tr></thead>
      <tbody id="histBody"><tr><td colspan="5" style="color:var(--muted);text-align:center;padding:20px">Cargando...</td></tr></tbody>
    </table>
  </div>
</div>

<!-- MODAL MOVIMIENTO -->
<div class="overlay" id="movOverlay" onclick="cerrarOverlayClick(event,'movOverlay')">
 <div class="modal">
  <div class="mtitle">Nuevo movimiento</div>

  <!-- Paso 1: tipo -->
  <div id="p1">
    <div class="plbl">¿Qué registrás?</div>
    <div class="tipo-grid">
      <button class="tbtn ing" id="btnIng" onclick="selTipo('Ingreso')"><span class="ico">📈</span>Ingreso</button>
      <button class="tbtn egr" id="btnEgr" onclick="selTipo('Egreso')"><span class="ico">📉</span>Egreso</button>
    </div>
  </div>

  <!-- Paso 2A: subtipo ingreso -->
  <div id="p2i" style="display:none">
    <div class="dvdr"></div>
    <div class="plbl">Tipo de ingreso</div>
    <div class="sub-grid">
      <button class="sbtn" onclick="selSub(this,'Punto de Venta')"><span class="ico">🏧</span>Punto de Venta</button>
      <button class="sbtn" onclick="selSub(this,'PagoMóvil')"><span class="ico">📱</span>PagoMóvil</button>
      <button class="sbtn" onclick="selSub(this,'BioPago')"><span class="ico">👆</span>BioPago</button>
      <button class="sbtn" onclick="selSub(this,'Efectivo Bs')"><span class="ico">💵</span>Efectivo Bs</button>
      <button class="sbtn dol" onclick="selSub(this,'Efectivo $')" style="grid-column:1/-1"><span class="ico">💲</span>Efectivo en Dólares</button>
    </div>
  </div>

  <!-- Paso 2B: categoría egreso -->
  <div id="p2e" style="display:none">
    <div class="dvdr"></div>
    <div class="plbl">Categoría</div>
    <div class="cat-grid">
      <button class="cbtn" id="btnCasa" onclick="selCat('Casa')"><span class="ico">🏠</span>Casa</button>
      <button class="cbtn" id="btnTrab" onclick="selCat('Trabajo')"><span class="ico">💼</span>Trabajo</button>
    </div>
  </div>

  <!-- Paso 3: concepto egreso -->
  <div id="p3e" style="display:none">
    <div class="dvdr"></div>
    <div class="plbl" id="p3lbl">Concepto</div>
    <div class="con-grid" id="conGrid"></div>
  </div>

  <!-- Monto -->
  <div id="pMonto" style="display:none">
    <div class="dvdr"></div>
    <div class="fld">
      <div class="flbl" id="montoLbl">Monto en Bolívares</div>
      <input type="text" class="finp" id="montoInp" placeholder="0,00" inputmode="decimal">
    </div>
    <button class="btn-save" onclick="guardarMov()">✓ Guardar</button>
  </div>

  <button class="btn-can" onclick="cerrarModal('movOverlay')">Cancelar</button>
 </div>
</div>

<!-- MODAL EDITAR MONTO -->
<div class="dp-overlay" id="editOverlay" onclick="cerrarOverlayClick(event,'editOverlay')">
  <div class="dp-box" style="max-width:420px">
    <div class="dp-title">✏️ Editar monto</div>
    <div style="font-size:30px;color:var(--muted);margin-bottom:8px;text-align:center" id="editConcepto"></div>
    <div style="font-size:26px;color:var(--muted);margin-bottom:14px;text-align:center" id="editLbl">Monto en Bolívares</div>
    <input type="text" class="dp-inp" id="editInp" placeholder="0,00" inputmode="decimal">
    <div class="dp-btns">
      <button class="dp-cancel" onclick="cerrarModal('editOverlay')">Cancelar</button>
      <button class="dp-ok" onclick="guardarEdicion()">Guardar</button>
    </div>
  </div>
</div>

<!-- DATEPICKER -->
<div class="dp-overlay" id="dpOverlay" onclick="cerrarOverlayClick(event,'dpOverlay')">
  <div class="dp-box">
    <div class="dp-title">Ir a una fecha</div>
    <input type="date" class="dp-inp" id="dpInp">
    <div class="dp-btns">
      <button class="dp-cancel" onclick="cerrarModal('dpOverlay')">Cancelar</button>
      <button class="dp-ok" onclick="aplicarFecha()">Ir</button>
    </div>
  </div>
</div>

<!-- SPINNER -->
<div class="spin" id="spin"><div class="ring"></div><span id="spinMsg">Cargando...</span></div>
<!-- TOAST -->
<div class="toast" id="toast"></div>

<script src="app.js"></script>
</body>
</html>
